[Service]
# sirius server mode: thd must run as root (backlight/fb/state need it).
# Default Debian runs --user nobody, which can only observe, not control.
ExecStart=
ExecStart=/usr/sbin/thd --triggers /etc/triggerhappy/triggers.d/ --socket /run/thd.socket --user root --deviceglob /dev/input/event*
