# tqftpserv（上游源码占位，不 vendor）

上游：https://github.com/andersson/tqftpserv
本机没有源码时：

```bash
git clone https://github.com/andersson/tqftpserv
```

说明：手机上跑的是调试版 `tqftpserv-dbg`（去 `-d` 参数，
见服务器 `tqftpserv-src/` 与 docs）。如上游对不上版本，
以手机现行二进制为准，差异记到这里来（TODO：核对上游版本）。